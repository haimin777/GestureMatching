# GestureMatching
Gesture matching app

clone repository
install requirements

run 
'''python app.py'''

First stage - register gestures from video. Saved frames could be finded in folder `frames`
Second stage - matching video, with registered gestures. Pairwise results are saved in folder `match`
